<p id="notice"><%= notice %></p>

<h1>Publications</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Book</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @publications.each do |publication| %>
      <tr>
        <td><%= User.where(id: publication.user_id).last.username %></td>
        <td><%= Book.where(id: publication.book_id).last.title %></td>
        <td><%= link_to 'Show', publication %></td>
        <td><%= link_to 'Edit', edit_publication_path(publication) %></td>
        <td><%= link_to 'Destroy', publication, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Publication', new_publication_path %>
<%= link_to 'New Book', new_book_path %>
<%= link_to 'New User', new_user_path %>

<br>

<%= link_to 'Books', books_path %>
<%= link_to 'Users', users_path %>